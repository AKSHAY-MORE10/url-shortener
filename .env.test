# Supabase PostgreSQL (Prisma)
# Connect to Postgres via the shared transaction-mode pooler (IPv4-only)
DATABASE_URL="postgresql://postgres.dilbfkbpjwdcdyyopyna:WEARERUDE102005@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&schema=url_shortener_test"

# Connect to Postgres via the shared session-mode pooler (used for migrations)
DIRECT_URL="postgresql://postgres.dilbfkbpjwdcdyyopyna:WEARERUDE102005@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?schema=url_shortener_test"
# Server
PORT=3001
NODE_ENV=test

# Application
BASE_URL="http://localhost:3000"

# Redis
REDIS_URL="redis://localhost:6379/1"

# Auth (Phase 6+, generate with: openssl rand -hex 32)
JWT_SECRET="<paste generated secret here>"

